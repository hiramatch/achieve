<p id="notice"><%= notice %></p>
<div class="child row">
  <div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <h2>メッセージ</h2>
    <div class="blog-text">
      <p class="left"><%= @submit_request.message %></p>
   </div>
  </div>
</div>
<%= link_to '戻る', user_submit_requests_path(current_user), class: 'btn btn-default' %>
